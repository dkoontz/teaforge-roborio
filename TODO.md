- [ ] The teaforge project has been updated to allow the logging of program state to be used by an external debugger. The type for the program config record has changed. I have built the latest version (0.1.6) of teaforge and copied it into this project. Now I want to make the updates necessary to be compatible with the debugging system. Entries should be appended to a file in the application's directory as they are received and the buffer flushed immediately. Debugging should always be enabled for now, in the future we will make this configurable by the application. Since there can be multiple log files in the program's directory, let's create them according to the current date/time such as `2025-02-11_14.23.18-debug-log.jsonl`. A new file should be created each time the application is run.
- [ ] 